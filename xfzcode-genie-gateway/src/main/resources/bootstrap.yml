server:
  port: 18080
spring:
  application:
    name: genie-gateway
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        #配置中心服务地址
        server-addr: api.xfzcode.com:30158
        #配置中心文件类型后缀
        file-extension: yaml
        #配置中心编码
        encode: utf-8
