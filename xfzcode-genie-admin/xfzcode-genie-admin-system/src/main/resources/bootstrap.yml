server:
  port: 18080
  servlet:
    context-path: /admin
spring:
  application:
    name: genie-admin
  profiles:
    active: outNet
  cloud:
    nacos:
      config:
        #配置中心服务地址
        server-addr: api.xfzcode.com:30158
        #配置中心文件类型后缀
        file-extension: yaml
        #配置中心编码
        encode: utf-8

